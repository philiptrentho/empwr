{
    "name": "ts-fb-monorepo",
    "version": "0.0.1",
    "workspaces": [
        "web",
        "shared",
        "functions"
    ],
    "scripts": {
        "build": "npm run build --workspaces",
        "test": "npm run test --workspaces"
    },
    "dependencies": {
        "@types/markdown-it": "^14.1.1",
        "linkify-it": "^5.0.0",
        "rollup": "^4.17.2",
        "typescript": "^5.4.5"
    }
}
